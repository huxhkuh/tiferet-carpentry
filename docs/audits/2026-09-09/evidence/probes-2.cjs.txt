const {chromium}=require(process.cwd()+'/node_modules/playwright');const AxeBuilder=require(process.cwd()+'/node_modules/@axe-core/playwright').default;const fs=require('node:fs'),path=require('node:path'),zlib=require('node:zlib');const out=path.join(process.env.TEMP,'tiferet-audit-20260909');
(async()=>{const browser=await chromium.launch({headless:true});const context=await browser.newContext({viewport:{width:1440,height:1000}});const page=await context.newPage();let result={};const errors=[];page.on('pageerror',e=>errors.push(String(e)));await page.goto('http://127.0.0.1:5198/tiferet-carpentry/design/bedroom');await page.getByRole('button',{name:'גרסאות ושיתוף',exact:true}).click();await page.getByRole('dialog').waitFor();result.libraryFocus={inside:await page.evaluate(()=>!!document.activeElement.closest('[role=dialog]'))};await page.keyboard.press('Escape');result.libraryFocus.escapeCloses=await page.getByRole('dialog').count()===0;result.dialogAxe=(await new AxeBuilder({page}).withTags(['wcag2a','wcag2aa','wcag21a','wcag21aa']).analyze()).violations.map(v=>({id:v.id,impact:v.impact,nodes:v.nodes.length}));
const content='0 g 10 10 20 100 re f\n10 10 m 20 20 l 30 30 l 40 40 l\n';const compressed=zlib.deflateSync(content);const binary=Buffer.concat([Buffer.from('%PDF-1.4\n<< /Length '+compressed.length+' /Filter /FlateDecode >>\nstream\n'),compressed,Buffer.from('\nendstream\n%%EOF')]);
result.engine=await page.evaluate(async({binary})=>{const root='/tiferet-carpentry/src/';const m=await import(root+'engine/materials.ts');const partModule=await import(root+'engine/parts.ts');const pdf=await import(root+'apartment/import/pdf-import.ts');const customStore=await import(root+'store/custom-materials-store.ts');const store=await import(root+'store/cabinet-store.ts');const grain=await import(root+'engine/cut-optimizer.ts');const step=await import(root+'engine/export/step-export.ts');const gcode=await import(root+'utils/gcode-export.ts');
const custom={...m.MATERIALS[0],key:'audit-custom-19',thickness:19,name:{he:'חומר בדיקה',en:'Audit custom'}};customStore.useCustomMaterialsStore.getState().addMaterial(custom);let customError;try{store.useCabinetStore.getState().setConfig({carcassMaterial:custom.key});customError=null}catch(e){customError=String(e)}
const analyzed=await pdf.analyzeArchitecturalPdf(new File([new Uint8Array(binary)],'audit.pdf',{type:'application/pdf'}));const part={id:'P1',qty:1,name:{en:'audit',he:'בדיקה'},material:m.MATERIALS[0].key,thickness:m.MATERIALS[0].thickness,length:800,width:300,edgeBanding:{en:'none',he:'ללא'},grainConstraint:'along-width'};const optimized=grain.optimizeCutSheets([part]);
const decimalPart={...part,length:800.5,width:300.25};const stepExports=Object.keys(step);let stepResult;const generate=step.generateStepContent;if(generate){const generated=generate({...m.DEFAULT_CONFIG},[decimalPart]);const text=generated.content;stepResult=typeof text==='string'?text.split('\n').filter(l=>/\d+\.\d+\./.test(l)).slice(0,3):text;}
return{customMaterialError:customError,legacyCompressedPdf:analyzed,grainInput:part,grainOutput:optimized.sheets[0]?.parts[0],sawKerf:m.SAW_KERF,stepExports,decimalStep:stepResult,gcodeStart:gcode.cutSheetToGcode(optimized.sheets[0]).split('\n').slice(10,20)};},{binary:[...binary]});
await page.goto('http://127.0.0.1:4198/tiferet-carpentry/');await page.getByRole('heading',{name:'נגרות מדויקת. בתים מעוררי השראה.'}).waitFor();await page.waitForTimeout(800);result.production={serviceWorkers:await page.evaluate(async()=>(await navigator.serviceWorker.getRegistrations()).map(r=>r.scope))};await page.context().setOffline(true);try{await page.reload({timeout:10000,waitUntil:'domcontentloaded'});result.production.offlineLoaded=await page.getByRole('heading',{name:'נגרות מדויקת. בתים מעוררי השראה.'}).isVisible()}catch(e){result.production.offlineLoaded=false;result.production.offlineError=String(e).split('\n')[0]}await page.context().setOffline(false);
try{const live=await page.goto('https://huxhkuh.github.io/tiferet-carpentry/',{timeout:30000,waitUntil:'domcontentloaded'});result.live={status:live?.status(),title:await page.title()};}catch(e){result.live={error:String(e).split('\n')[0]}}
result.errors=errors;fs.writeFileSync(path.join(out,'probes-2.json'),JSON.stringify(result,null,2));console.log(JSON.stringify(result,null,2));await browser.close();})().catch(e=>{console.error(e);process.exitCode=1});
